services:
  db:
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
    environment:
      POSTGRES_DB: microloans_staging

  api:
    env_file:
      - .env.staging
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'
